package dev.esnault.wanakana


/**
 * A mode to handle conversion while the text is being typed.
 *
 * TODO Add some documentation here about each mode.
 */
enum class IMEMode {
    DISABLED,
    ENABLED,
    TO_HIRAGANA,
    TO_KATAKANA
}
